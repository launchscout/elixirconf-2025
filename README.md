This is the slide deck for the ElixirConf 2025 session "Extending the Reach of Elixir with WebAssembly Components."

To build the slides:

```
npm run build
```

To serve them:

```
npm start
```

The example project repo is [wasm_commerce](https://github.com/launchscout/wasm_commerce)